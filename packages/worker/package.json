{
  "name": "@hexed/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./worker": "./src/worker.ts"
  },
  "scripts": {
    "build": "node scripts/build-worker.js",
    "build:worker": "npm run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@hexed/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@vitest/ui": "^2.1.8",
    "@vitest/coverage-v8": "^2.1.8",
    "esbuild": "^0.24.0",
    "happy-dom": "^15.11.6",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
