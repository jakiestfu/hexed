/**
 * Worker entry point for the web app
 * This file imports the Worker from @hexed/worker
 * Vite will bundle this as a separate Worker file
 */

// Import the worker - this will be bundled by Vite
import "@hexed/worker/worker"
